<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wheny.wenyapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test_coroutines.TestCoroutinesActivity"></activity>
        <activity android:name=".mvvm.view.TestFragment.TestFragmentActivity" />
        <activity android:name=".mvvm.view.databinding.TestDataBindingActivity" />
        <activity android:name=".mvvm.view.list.TestListActivity" />
        <activity android:name=".mvvm.view.login.LoginActivity" />
        <activity android:name=".mvvm.view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>